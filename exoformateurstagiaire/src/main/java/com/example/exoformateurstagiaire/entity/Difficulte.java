package com.example.exoformateurstagiaire.entity;

public enum Difficulte {
    FACILE, MOYEN, DIFFICILE;
    // avec Enum, on considère que dans la colonne Difficulté,
    // les cellules ne pourront être rempli QUE par les éléments renseignés ici (facile, moyen, difficile)
}
